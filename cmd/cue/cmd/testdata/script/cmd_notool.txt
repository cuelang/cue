! cue cmd notool
! stdout .
cmp stderr cmd_baddisplay.out

-- cmd_baddisplay.out --
command.notool.task.display: invalid value string (mismatched types string and int)
-- task.cue --
package home
message: "Hello world!"

command: notool: {
	task: display: {
		kind: "print"
		text: 42
	}
}
